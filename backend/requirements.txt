# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Pydantic for settings
pydantic==2.5.3
pydantic-settings==2.1.0

# Database
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
alembic==1.13.1

# Redis and Celery
redis==5.0.1
celery==5.3.6

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 is incompatible with bcrypt >= 4.1 (reads removed bcrypt.__about__);
# pin to the last 4.0.x which passlib supports.
bcrypt==4.0.1
python-dotenv==1.0.0

# HTTP clients for OAuth and cloud APIs
httpx==0.26.0
requests==2.31.0

# AWS SDK
boto3==1.34.34

# Encryption
cryptography==42.0.0

# Twilio for SMS
twilio==8.12.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
aiosqlite==0.19.0
